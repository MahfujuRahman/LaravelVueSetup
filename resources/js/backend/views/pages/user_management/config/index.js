let prefix = "user";

let config = {
    all_page_title: prefix + " management",
    create_page_title: prefix + " management",
    edit_page_title: prefix + " management",
    show_page_title: prefix + " management",

    route_name_prefix: "User",
    route_path_prefix: "/user",

    api_host: "http://localhost:8000",
    api_version: "/api/v1",
    api_path_prefix: "/users/auth-management",

};

export default config;
